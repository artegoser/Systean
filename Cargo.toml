[package]
name = "systean"
version = "0.1.0"
edition = "2024"
description = "Core engine for the Systean unambiguous artificial language"
license = "MIT"

[dependencies]
chumsky = "=0.13.0"
serde = { version = "1", features = ["derive"] }
toml = "0.9.9"

[lib]
path = "src/lib.rs"
