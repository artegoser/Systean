# Phase 20 — English reference documentation and controlled rendering

Status: **planned**
Depends on: Phases 18–19

## Goal

Make the complete public Systean vocabulary learnable and explainable in English, and provide one deterministic English rendering path for Systean expressions without making English part of normative Systean semantics.

English is the fixed bridge language for Systean 1.0. Other natural-language documentation/renderers are explicitly deferred until they can be designed as independent consumers of canonical semantics rather than as naive substitutions over English.

## Per-word documentation contract

Every public word must have a compiled English documentation entry.

Required fields:

- [ ] `gloss`: short English equivalent suitable for search results and hover UI;
- [ ] `explain`: precise English explanation of the semantic contribution;
- [ ] canonical semantic signature, included by reference from the compiled declaration rather than duplicated as prose data;
- [ ] pronunciation/stress, included from the phonology engine rather than duplicated;
- [ ] at least one canonical Systean example;
- [ ] controlled English rendering for the example generated by the renderer, not manually allowed to drift silently.

Where applicable, the explanation must state:

- [ ] argument roles in human terms;
- [ ] what the word does **not** imply when that distinction matters;
- [ ] scope behavior;
- [ ] discourse/reference/context effect;
- [ ] whether the declaration is primitive, defined, or intrinsic-backed;
- [ ] relevant contrast words that users are likely to confuse.

Operators/constructions require at least one example that makes their scope/attachment behavior visible.

## Documentation source format

Human documentation is stored separately from normative semantic declarations, tentatively under:

```text
language/docs/en.sydoc
```

Representative shape:

```text
word vid {
    gloss "see";
    explain """
    Expresses visual perception by the first participant of the second participant.
    It does not itself assert recognition, belief, attention, or duration.
    """;

    example "mi vid tu";
}
```

The documentation compiler resolves `vid` to the compiled symbol and rejects unknown or duplicate entries.

The source format must support splitting documentation into multiple files/modules once the vocabulary grows, without changing symbol identity.

## Coverage compiler

- [ ] Package compilation exposes the set of public documented words/constructions.
- [ ] A release check fails if a public 1.0 word lacks English `gloss` or `explain`.
- [ ] Examples are parsed through the real Systean engine.
- [ ] Example canonical semantics are stored/checked so a grammar change cannot silently change the teaching material.
- [ ] Documentation has an independent fingerprint.
- [ ] Documentation-only edits do not change semantic/surface fingerprints.

## Controlled English renderer

The renderer consumes canonical typed semantic structures, plus explicit discourse information where required, and produces deterministic English.

It is **not** word-for-word substitution.

Required properties:

- [ ] compositional rendering for ordinary predicates/relations;
- [ ] explicit rendering for logical negation/conjunction/disjunction;
- [ ] quantifier rendering preserving scope;
- [ ] controlled rendering for counted/generic/statistical claims;
- [ ] explicit rendering for information status (`unknown`, `unspecified`, `withheld`) without inventing missing content;
- [ ] context/reference rendering that uses resolved referents when a stable human label exists and otherwise remains explicit;
- [ ] date/time/quantity/unit rendering from typed values;
- [ ] speech-act/repair rendering that distinguishes propositional content from communicative/discourse effect;
- [ ] deterministic parenthesization or explanatory phrasing where ordinary English would otherwise hide Systean scope;
- [ ] no LLM/paraphraser in normative renderer tests.

## Controlled English vs idiomatic English

The site/API must label the deterministic output simply as `English` or `English rendering`, with documentation explaining that fidelity is prioritized over stylistic naturalness.

A future optional `natural paraphrase` feature may exist, but:

- it is non-normative;
- it cannot replace controlled English in tests;
- it cannot be used to decide Systean parse/meaning;
- it must be visually separated from deterministic rendering.

## Search/index data

The documentation compiler produces a user-facing search index containing at least:

- root;
- English gloss;
- English explanation text;
- semantic declaration kind;
- semantic result type;
- argument count/types;
- semantic/domain tags if manually declared as documentation metadata;
- examples;
- source/documentation provenance.

Search metadata must not be treated as semantic identity.

## Required validation

- [ ] 100% of public 1.0 roots have English glosses and explanations.
- [ ] All documentation examples parse and round-trip.
- [ ] Controlled English rendering has golden tests across every major semantic subsystem.
- [ ] Rendering never changes canonical Systean semantic terms.
- [ ] English documentation edits alone change only the documentation fingerprint.
- [ ] The renderer has explicit tests for ambiguous-looking English cases where Systean scope must remain visible.

## Completion result

Every public Systean word is teachable in English, every analyzed expression can receive a deterministic English interpretation, and no English wording participates in normative semantic identity.
